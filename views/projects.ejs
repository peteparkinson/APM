<!DOCTYPE html>
<html>

<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='stylesheets/style.css'>

    <script>

        const allProStr = JSON.parse('<%- proStr %>');
        const allMatStr = JSON.parse('<%- matStr %>');

        function toggle() {
            var check = document.getElementById('check');
            console.log(check.value);
        }

        function showDetails() {
            document.getElementById('form1').reset();
            var selection = document.getElementById('openProjects').value;
            allProStr.objects.forEach(e1 => {
                if (selection == e1.name) {
                    document.getElementById("hours").value = e1.hours;
                    document.getElementById("charge").value = e1.charge;
                    document.getElementById("customer").value = e1.customer;

                    //calculate materials cost
                    if (e1.relMaterials) {
                        var matCost = 0;
                        allMatStr.objects.forEach(e2 => {
                            if (e1.relMaterials.includes(e2.name)) {
                                //check multiple occurrences of a meterial in a project
                                let occ = 0;
                                for (let i = 0; i < e1.relMaterials.length; i++) {
                                    if (e1.relMaterials[i] == e2.name) {
                                        occ++;
                                    }
                                }
                                matCost += (e2.cost * occ);
                            }
                        });
                        document.getElementById("matCost").value = Math.floor(matCost * 100) / 100;
                    }
                    //document.getElementById("total").value = calcTotalCost(e);
                    //document.getElementById("net").value = calcNet(e);
                }
            });
        }

        function calcMatCost(project) {

        }
        function calcTotalCost(project) {

        }
        function calcNet(project) {

        }
    </script>
</head>

<body>
    <h1><%= title %></h1>
    <% include  templates/header.ejs%>

    <!-- Column 1 -->
    <form class="forms">
        <h1>Closed Projects</h1>
        </label>
        <select class="box" id="closedProjects" size="999">
            <% allProjects.objects.forEach(function(item){ %>
            <% if(item.status == "closed"){ %>
            <option value="<%= item.name %>"> <%= item.name %> </option>
            <% }});%>
        </select>
        <input type="button" value="Reopen Project -->" style="margin-top: 20px; float: right;">
    </form>

    <!-- Column 2 -->
    <form class="forms">
        <h1> Open Projects </h1>
        <select class="box" id="openProjects" size="999" onclick="showDetails()" onkeyup="showDetails()">
            <% allProjects.objects.forEach(function(item){ %>
            <% if(item.status == "open"){ %>
            <option value="<%= item.name %>"> <%= item.name %> </option>
            <% }});%>
        </select>
        <input type="submit" value="Submit" formaction="/projects/submitted" style="float: right; margin-top: 20px">
        <input type="button" value="<- Close" style="margin-top: 20px;">
    </form>

    <!-- Column 3 -->
    <form class="forms" id="form1">
        <h1> Costs </h1>
        <ul>
            <li>
                <label for="hours">Hours</label>
                <input type="number" id="hours" name="hours" maxlength="3" min="0" max="999" oninput="javascript: 
                    if (this.value.length > 3) this.value = this.value.slice(0, this.maxLength);">
            </li>
            <li>
                <label for="materialCost">Materials</label>
                <input type="number" id="matCost" name="materialCost" step="0.01" maxlength="3" min="0.01" max="999">
            </li>
            <li>
                <label for="total">Total</label>
                <input type="number" id="total" name="total" step="0.01" maxlength="3" min="0.01" max="999">
            </li>
            <h1> Return </h1>
            <li>
                <label for="charge">Charge</label>
                <input type="number" id="charge" name="charge" step="0.01" maxlength="3" min="0.01" max="999">
            </li>
            <li>
                <label for="net">Net</label>
                <input type="number" id="net" name="net" step="0.01" maxlength="3" min="0.01" max="999">
            </li>
            <br><br>
            <li style="border:1px solid rgb(221, 221, 221);">
                <label for="customer">Customer</label>
                <input type="text" id="customer" name="customer" maxlength="30">
            </li>
        </ul>
    </form>
</body>

</html>